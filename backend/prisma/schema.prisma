// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("TEST_DATABASE_URL")
}

model User {
  id               String     @id @unique @default(uuid())
  username         String     @unique
  password         String
  sentMessages     Messages[] @relation("SentMessages")
  receivedMessages Messages[] @relation("ReceivedMessages")
  chatroom         Chatroom[]
}

model Messages {
  id             String   @id @default(uuid())
  text           String
  timestamp      DateTime @default(now())
  user           User     @relation("SentMessages", fields: [userId], references: [id])
  userId         String
  sentMessages   User     @relation("ReceivedMessages", fields: [sentMessagesId], references: [id])
  sentMessagesId String
  chatroom       Chatroom @relation(fields: [chatId], references: [id])
  chatId         String
}

model Chatroom {
  id       String     @id @default(uuid())
  messages Messages[]
  users    User[]
}
